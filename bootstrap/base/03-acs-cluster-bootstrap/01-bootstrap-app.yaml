apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: acs-cluster-config-bootstrap
  namespace: openshift-gitops
spec:
  generators:
    - clusterDecisionResource:
        configMapRef: acm-placement
        labelSelector:
          matchLabels:
            cluster.open-cluster-management.io/placement: acm-placement
        requeueAfterSeconds: 180
  template:
    metadata:
      name: acs-bootstrap-{{name}}
    #  labels:
    #    velero.io/exclude-from-backup: "true"
    spec:
      project: acs-bootstrap
      source:
        repoURL: https://github.com/redhatryan/cluster-config.git
        targetRevision: main
        path: bootstrap/overlays/{{ fromClusterClaim "acs" }}
      destination:
        namespace: openshift-gitops
        server: "{{server}}"
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
        syncOptions:
          - CreateNamespace=true
          - PruneLast=true
